name: Test Flutter Setup

on:
  push:
    branches:
      - develop
  pull_request:
    branches:
      - develop

jobs:
  test-setup:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - uses: open-ci-io/flutter-setup@v1
        with:
          flutter-version: "stable"

      - name: Verify Flutter
        run: flutter --version
