name: Demo Workflow

on:
  push:
    branches:
      - develop
  pull_request:
    branches:
      - develop

jobs:
  build:
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install dependencies
        run: flutter pub get
