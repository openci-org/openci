name: openci_worker_cli
description: OpenCI Worker CLI
version: 0.5.0
publish_to: none
repository: https://github.com/open-ci-io/openci

environment:
  sdk: ^3.11.0

resolution: workspace

dependencies:
  args: ^2.7.0
  dart_avf:
    path: ../../packages/dart_avf
  http: ^1.6.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6

executables:
  openci_worker: openci_worker_cli
