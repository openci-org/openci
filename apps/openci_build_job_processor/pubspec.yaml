name: openci_build_job_processor
description: OpenCIのビルドジョブを処理するDartプログラムです。
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.11.5

resolution: workspace

dependencies:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.24.0
