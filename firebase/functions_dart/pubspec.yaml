name: functions_dart
description: Dart Cloud Functions for OpenCI
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: "3.11.5"

dependencies:
  firebase_functions: ^0.6.0
  firebase_admin_sdk: ^0.5.3
  google_cloud_firestore: ^0.5.2
  collection: ^1.19.1
  shelf: ^1.4.1
  openci_shared:
    path: ../../packages/openci_shared
  gcp_secret_manager:
    path: ../../packages/gcp_secret_manager
  dart_jsonwebtoken: ^3.2.0

dev_dependencies:
  build_runner: ^2.15.0
