specVersion: v1
serviceId: openci
location: asia-northeast1
schemaValidation: "COMPATIBLE"
schema:
  source: ./schema
  datasource:
    postgresql:
      database: openci
      cloudSql:
        instanceId: openci
connectorDirs:
  - ./connector
