{
  "name": "ima_functions",
  "version": "1.0.0",
  "description": "Ima Cloud Functions",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "test": "npm run build && rm -rf /tmp/ima-functions-test-lib && tsc -p tsconfig.test.json && NODE_PATH=$(pwd)/node_modules node --test \"/tmp/ima-functions-test-lib/**/*.test.js\""
  },
  "keywords": [],
  "author": "",
  "license": "UNLICENSED",
  "private": true,
  "engines": {
    "node": "22"
  },
  "dependencies": {
    "@cursor/sdk": "^1.0.12",
    "firebase-admin": "^13.8.0",
    "firebase-functions": "^7.2.5"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^6.0.3"
  }
}
