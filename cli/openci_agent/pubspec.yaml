name: openci_agent
description: OpenCI Agent — REST API for macOS VM lifecycle on physical Mac hardware
publish_to: none
version: 0.1.0
repository: https://github.com/open-ci-io/openci

environment:
  sdk: ^3.11.0

resolution: workspace

dependencies:
  dart_avf:
    path: ../../packages/dart_avf
  relic: ^1.1.0
  args: ^2.7.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
