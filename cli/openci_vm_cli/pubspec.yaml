name: openci_vm_cli
description: OpenCI VM CLI
version: 1.0.7
repository: https://github.com/open-ci-io/openci
homepage: https://openci.io

environment:
  sdk: ^3.6.0

dependencies:
  args: ^2.7.0
  http: ^1.6.0
  jose: ^0.3.5

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6

executables:
  openci_vm: openci_vm_cli
