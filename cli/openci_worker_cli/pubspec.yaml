name: openci_worker_cli
description: OpenCI Worker CLI
version: 0.4.0
repository: https://github.com/open-ci-io/openci

environment:
  sdk: ^3.10.7

dependencies:
  args: ^2.7.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
