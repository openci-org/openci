coverage:
  status:
    project:
      default:
        informational: true
    patch:
      default:
        informational: true

comment:
  layout: "reach, diff, flags, files"
  behavior: default
  require_changes: false

ignore:
  - "**/*.g.dart"
  - "**/*.freezed.dart"
  - "**/*.chopper.dart"

flags:
  openci_cli:
    paths:
      - apps/openci_cli/lib/
  openci_workflow:
    paths:
      - packages/openci_workflow/lib/
  build_job_planner:
    paths:
      - apps/build_job_planner/lib/
  build_job_worker:
    paths:
      - apps/build_job_worker/lib/
  openci_server:
    paths:
      - apps/openci_server/lib/
      - apps/openci_server/routes/
  openci_shared:
    paths:
      - packages/openci_shared/lib/
