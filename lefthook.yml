pre-commit:
  parallel: true
  commands:
    lint:
      glob: "server/**/*.{ts,tsx,js,jsx}"
      run: cd server && pnpm oxlint {staged_files}
    typecheck:
      run: cd server && pnpm typecheck

pre-push:
  commands:
    build:
      run: cd server && pnpm build
