name: openci_shared
description: Shared constants and utility models for OpenCI organization.
version: 1.0.0
repository: https://github.com/openci-org/openci/tree/main/packages/openci_shared
resolution: workspace

environment:
  sdk: "3.11.5"

dependencies:
  freezed_annotation: ^3.1.0
  json_annotation: ^4.11.0

dev_dependencies:
  build_runner: ^2.15.0
  freezed: ^3.2.5
  json_serializable: ^6.13.0
