{
  "name": "openci",
  "private": true,
  "workspaces": [
    "apps/landing_page"
  ],
  "scripts": {
    "build": "vp run -r build",
    "check": "vp run -r check",
    "fmt": "vp run -r fmt",
    "fmt:check": "vp run -r fmt:check"
  },
  "devDependencies": {
    "vite-plus": "^0.1.21"
  },
  "packageManager": "npm@11.14.1"
}
